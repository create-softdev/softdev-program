const arr = ['a', 'b', 'c', 'd', 'e'];

for (let i = 0; i < arr.length; i += 1) {
  console.log(arr[i]);
}

let x = 2;

x *= 3;
console.log(x);

/**
 * increment and assign
 * Same for other operators, like "*"
 */
