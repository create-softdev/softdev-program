/**
 * Write a program that outputs the sum of `1`, `500`, and `3`, using a function
 * `sum(a, b, c)` that accepts three parameters and **returns** the sum of them all.
 *
 * Don't test the function.
 */
function sum (a, b, c) {
  // implement this function!
}

console.log(sum(1, 500, 3))
