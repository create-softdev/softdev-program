function greet (name) {
  return `The name is ${name.split(' ')[1]}. ${name}.`
}

console.log(greet('James Bond')) // The name is Bond. James Bond.

/**
 * string interpolation
 */
/* eslint no-console: 0 */
