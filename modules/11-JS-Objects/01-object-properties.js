const formerPresident = {
  firstName: 'Barack',
  lastName: 'Obama'
}

const currentPresident = {
  firstName: 'Donald',
  lastName: 'Trump'
}

console.log(formerPresident.firstName, formerPresident.lastName)

/**
 * object is between {...}
 * A set of **properties**, where each property is associated with one **value**
 * properties - only strings. values - whatever we want
 * accessing properties is with the '.' operator
 *
 */
