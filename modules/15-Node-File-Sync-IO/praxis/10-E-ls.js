/**
 * Write 'ls', so that it will show as many files in a row as possible.
 * It will read all the files, determine the one with the longest length....
 *
 * Use process.stdout.columns to determine the width of the terminal
 */
